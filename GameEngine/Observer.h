#pragma once

namespace bombGame
{
	class Observer
	{
	public:
		Observer() = default;
		virtual ~Observer() = default;

		//virtual void Notify(Event event, GameActor*)

	protected:

	private:

	};
}